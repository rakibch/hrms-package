export enum BrandPermission {
    ALL = 'brand.*',
    VIEW = 'brand.view',
    ADD = 'brand.add',
    UPDATE = 'brand.update',
    DELETE = 'brand.delete',
}

export enum CategoryPermission {
    ALL = 'category.*',
    VIEW = 'category.view',
    ADD = 'category.add',
    UPDATE = 'category.update',
    DELETE = 'category.delete',
}

export enum DashboardPermission {
    ALL = 'dashboard.*',
    TOP_SELLING_PRODUCT = 'dashboard.top-selling-product',
    SALES_SUMMARY = 'dashboard.sales-summary',
    PURCHASE_SUMMARY = 'dashboard.purchase-summary',
    RECENT_SALES = 'dashboard.recent-sales',
    LOW_QUANTITY_STOCK = 'dashboard.low-quantity-stock',
}

export enum ProductPermission {
    ALL = 'product.*',
    VIEW = 'product.view',
    ADD = 'product.add',
    UPDATE = 'product.update',
    DELETE = 'product.delete',
}

export enum TagPermission {
    ALL = 'tag.*',
    VIEW = 'tag.view',
    ADD = 'tag.add',
    UPDATE = 'tag.update',
    DELETE = 'tag.delete',
}

export enum VariantOptionPermission {
    ALL = 'variant-option.*',
    VIEW = 'variant-option.view',
    ADD = 'variant-option.add',
    UPDATE = 'variant-option.update',
    DELETE = 'variant-option.delete',
}

export enum BranchPermission {
    ALL = 'branch.*',
    VIEW = 'branch.view',
    ADD = 'branch.add',
    UPDATE = 'branch.update',
    DELETE = 'branch.delete',
}

export enum SupplierPermission {
    ALL = 'supplier.*',
    VIEW = 'supplier.view',
    ADD = 'supplier.add',
    UPDATE = 'supplier.update',
    DELETE = 'supplier.delete',
}

export enum UserPermission {
    ALL = 'user.*',
    VIEW = 'user.view',
    ADD = 'user.add',
    UPDATE = 'user.update',
    DELETE = 'user.delete',
}

export enum RolePermission {
    ALL = 'Role.*',
    VIEW = 'Role.view',
    ADD = 'Role.add',
    UPDATE = 'Role.update',
    DELETE = 'Role.delete',
}

export enum SellPermission {
    ALL = 'sell.*',
    VIEW = 'sell.view',
    ADD = 'sell.add',
    UPDATE = 'sell.update',
    DELETE = 'sell.delete',
}

export enum OpenCloseRegisterPermission {
    ALL = 'open-close.*',
    VIEW = 'open-close.view',
    ADD = 'open-close.add',
    UPDATE = 'open-close.update',
    DELETE = 'open-close.delete',
}

export enum PurchaseOrderPermission {
    ALL = 'purchase-order.*',
    VIEW = 'purchase-order.view',
    ADD = 'purchase-order.add',
    UPDATE = 'purchase-order.update',
    DELETE = 'purchase-order.delete',
}

export enum LanguagePermission {
    ALL = 'language.*',
    VIEW = 'language.view',
    ADD = 'language.add',
    UPDATE = 'language.update',
    DELETE = 'language.delete',
}

export enum VatPermission {
    ALL = 'vat.*',
    VIEW = 'vat.view',
    ADD = 'vat.add',
    UPDATE = 'vat.update',
    DELETE = 'vat.delete',
}

export enum CurrencyPermission {
    ALL = 'currency.*',
    VIEW = 'currency.view',
    ADD = 'currency.add',
    UPDATE = 'currency.update',
    DELETE = 'currency.delete',
}

export enum ChannelPermission {
    ALL = 'channel.*',
    VIEW = 'channel.view',
    ADD = 'channel.add',
    UPDATE = 'channel.update',
    DELETE = 'channel.delete',
}
