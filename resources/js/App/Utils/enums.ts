export enum ROLES {
    ADMINISTRATOR = "Administrator",
    MANAGER = "Manager",
}

export enum CodeType {
    GTIN = "gtin",
    MPN = "mpn",
    EAN = "ean",
}

export enum CurrencyCode {
    BDT = "BDT",
    USD = "USD",
}

export enum SkuGenerationType {
    AUTOGENERATE = "auto-generate",
    MANUAL = "manual",
}
